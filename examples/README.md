# Examples

Runnable, self-contained examples. Each shows one clear use case.

- `basic/` — describe an expression and list its next runs.
- `convert/` — move a schedule between cron dialects, caveats and all.
